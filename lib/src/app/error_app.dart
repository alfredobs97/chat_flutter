import 'package:flutter/material.dart';

class ErrorApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Center(child: Text('Algo ha ido mal')),
    );
  }
}